#include <bits/stdc++.h>
using namespace std;

unordered_map<string,int> point;
point["CONTEST_WON"]=300;
point["TOP_CONTRIBUTOR"]=300;
point[""]
int main(int argc, char const *argv[])
{
	int tc;
	cin>>tc;
	while(tc--)
	{
		int n;
		cin>>n;
		string country;
		cin>>country;
		vector<string> v(n);
		for(int i=0;i<n;i++)
			getline(cin,v[i]);
		for(int i=0;i<n;i++)
		{
			v[i].split(" ");
		}

	}
	return 0;
}