#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(void)
{
	int tc;
	cin>>tc;
	while(tc--)
	{
		ll n,d;
		cin>>n>>d;
		ll arr[n];
		for(int i=0;i<n;i++)
			cin>>arr[i];
		sort(arr,arr+n);
		
		while(!d<=0)
		{
			
		}
	}
}
